services:
  chessarena:
    build: .
    image: chessarena:latest
    container_name: chessarena
    ports:
      - '${PORT:-8091}:3000'
    volumes:
      - chessarena-data:/data
    restart: unless-stopped

volumes:
  chessarena-data:
